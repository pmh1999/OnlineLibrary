var models = require("../models");
var controller = {};
let Catergory= models.Catergories;


controller.getAll=(query)=>{
    return new Promise((resovle,reject)=>{
        let option={
            
        }
        Category
    });
};
